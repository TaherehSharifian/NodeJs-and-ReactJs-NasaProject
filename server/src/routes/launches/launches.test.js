describe("Test GET /launches", () => {
  test("200 success respond", () => {
    const response = 200;
    expect(response).toBe(200);
  });
});

describe("Test POST /launches", () => {
  test("200 success respond", () => {});

  test("chacking missing required properties", () => {});

  test("chacking invalid dates", () => {});
});
